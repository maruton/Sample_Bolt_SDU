public enum ePlayerMainState {
    Init = 0,
    Stop,
    Walk,
    Run,
}
public enum ePlayerSubState {
    None = 0,
    Jump,
}

public enum ePlayerAnimation {
    Standing,
    Walk,
    Run,
    Jump,
    JumpLoop
}
